// Control Flow Exercise 1: Password Check

// 1. Create a variable, password
let password ="suspension"

/* 2. Write an if statement which console logs true if the 
password is over 7 characters or false otherwise. */
if (password.length > 7) {
    console.log(true);
}
else {
    console.log(false);
}
    // << result: true >> (since pw is over 7 letters) //