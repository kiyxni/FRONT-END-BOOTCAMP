// Number Exercise 4: Random Number 

// 1. Use the maths objects to create a random integer between 1 & 6.
let randomNumber = Math.floor(Math.random() * 6) +1;

// 2. Console.log your result.
console.log(randomNumber);
    // << result: always a random number between 1 & 6 >> //

